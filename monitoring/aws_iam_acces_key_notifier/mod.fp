mod "aws_iam_acces_key_notifier" {
  title         = "AWS IAM Access Key Notifier"
  // description   = "This module sends a e-mail notification when a new access key is created for an IAM user."
  documentation = file("./README.md")
  categories    = ["public cloud"]
}
